local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local Stroke = Instance.new("UIStroke")
local Corner = Instance.new("UICorner")
local TopBar = Instance.new("Frame")
local CloseBtn = Instance.new("TextButton")
local MinBtn = Instance.new("TextButton")

ScreenGui.Parent = game:GetService("CoreGui"):FindFirstChild("RobloxGui") or game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.Name = "MainGUI"

Frame.Parent = ScreenGui
Frame.Size = UDim2.new(0, 437, 0, 238)
Frame.Position = UDim2.new(0.5, -218.5, 0.5, -119)
Frame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Frame.BackgroundTransparency = 0.1
Frame.BorderSizePixel = 0
Frame.Visible = false
Frame.ClipsDescendants = true

-- Функция для добавления градиента
local function applyGradient(obj)
    local grad = Instance.new("UIGradient")
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(15, 15, 20)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(130, 90, 180))
    })
    grad.Rotation = 45
    grad.Parent = obj
end

applyGradient(Frame)

-- === НАСТРОЙКИ ГЛОБАЛЬНЫХ ПЕРЕМЕННЫХ ТЕМЫ И УВЕДОМЛЕНИЙ ===
_G.CurrentGradientColors = {
    Color3.fromRGB(15, 15, 20),
    Color3.fromRGB(130, 90, 180)
}
_G.NotificationsEnabled = true
_G.NotificationAssetID = "rbxthumb://type=Asset&id=115669221263328&w=150&h=150"
_G.MainMenuLoaded = false

-- Функция для добавления градиента (динамическая)
local function applyGradient(obj)
    local grad = Instance.new("UIGradient")
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, _G.CurrentGradientColors[1]),
        ColorSequenceKeypoint.new(1, _G.CurrentGradientColors[2])
    })
    grad.Rotation = 45
    grad.Parent = obj
end
_G.applyGradient = applyGradient

-- Функция динамического изменения темы для ВСЕХ окон
_G.changeTheme = function(color1, color2)
    _G.CurrentGradientColors = {color1, color2}
    for _, v in pairs(ScreenGui:GetDescendants()) do
        if v:IsA("UIGradient") then
            v.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, color1),
                ColorSequenceKeypoint.new(1, color2)
            })
        end
    end
end

local activeNotifications = {}
local NOTIF_BG_COLOR = Color3.fromRGB(18, 18, 22)
local NOTIF_STROKE_COLOR = Color3.fromRGB(210, 180, 255)
local TEXT_ACCENT_COLOR = Color3.fromRGB(220, 190, 255)

local function showNotification(message)
    if not _G.NotificationsEnabled then return end -- Проверка выключателя уведомлений
    
    local notifFrame = Instance.new("Frame")
    notifFrame.Size = UDim2.new(0, 290, 0, 75)
    notifFrame.Position = UDim2.new(1, 310, 1, -95)
    notifFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    applyGradient(notifFrame)
    notifFrame.BackgroundTransparency = 0.1
    notifFrame.BorderSizePixel = 0
    notifFrame.Parent = ScreenGui

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 10)
    corner.Parent = notifFrame

    local stroke = Instance.new("UIStroke")
    stroke.Color = NOTIF_STROKE_COLOR
    stroke.Thickness = 1.2
    stroke.Parent = notifFrame

    local girlIcon = Instance.new("ImageLabel")
    girlIcon.Size = UDim2.new(0, 55, 0, 55)
    girlIcon.Position = UDim2.new(0, 10, 0, 10)
    girlIcon.BackgroundTransparency = 1
    girlIcon.Image = _G.NotificationAssetID -- Динамический Asset ID
    girlIcon.Parent = notifFrame

    local imgCorner = Instance.new("UICorner")
    imgCorner.CornerRadius = UDim.new(0, 8)
    imgCorner.Parent = girlIcon

    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, -85, 0, 20)
    titleLabel.Position = UDim2.new(0, 75, 0, 10)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = "Notification"
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextSize = 14
    titleLabel.TextColor3 = TEXT_ACCENT_COLOR
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.Parent = notifFrame

    local msgLabel = Instance.new("TextLabel")
    msgLabel.Size = UDim2.new(1, -90, 0, 38)
    msgLabel.Position = UDim2.new(0, 75, 0, 27)
    msgLabel.BackgroundTransparency = 1
    msgLabel.Text = message
    msgLabel.Font = Enum.Font.Gotham
    msgLabel.TextSize = 12
    msgLabel.TextColor3 = Color3.fromRGB(240, 240, 240)
    msgLabel.TextWrapped = true
    msgLabel.TextXAlignment = Enum.TextXAlignment.Left
    msgLabel.TextYAlignment = Enum.TextYAlignment.Top
    msgLabel.Parent = notifFrame

    for _, oldNotif in ipairs(activeNotifications) do
        if oldNotif and oldNotif.Parent then
            local currentYOffset = oldNotif.Position.Y.Offset
            TweenService:Create(oldNotif, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                Position = UDim2.new(1, -300, 1, currentYOffset - 85)
            }):Play()
        end
    end

    table.insert(activeNotifications, notifFrame)

    TweenService:Create(notifFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Position = UDim2.new(1, -300, 1, -95)
    }):Play()

    task.delay(3.5, function()
        if not notifFrame or not notifFrame.Parent then return end
        TweenService:Create(notifFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Position = UDim2.new(1, 310, 1, notifFrame.Position.Y.Offset),
            BackgroundTransparency = 1
        }):Play()
        
        TweenService:Create(titleLabel, TweenInfo.new(0.2), {TextTransparency = 1}):Play()
        TweenService:Create(msgLabel, TweenInfo.new(0.2), {TextTransparency = 1}):Play()
        TweenService:Create(girlIcon, TweenInfo.new(0.2), {ImageTransparency = 1}):Play()
        TweenService:Create(stroke, TweenInfo.new(0.2), {Transparency = 1}):Play()

        task.wait(0.3)
        local index = table.find(activeNotifications, notifFrame)
        if index then table.remove(activeNotifications, index) end
        notifFrame:Destroy()
    end)
end

    
-- ИСПРАВЛЕННЫЙ makeDraggable без утечек памяти и зависаний
local function makeDraggable(guiObj, dragHandle)
    dragHandle = dragHandle or guiObj
    local dragging, dragInput, dragStart, startPos

    dragHandle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = guiObj.Position
        end
    end)

    dragHandle.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)

    UIS.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            if dragging then
                local delta = input.Position - dragStart
                guiObj.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
            end
        end
    end)
end

local function createToggle(parent, name, y)
    local label = Instance.new("TextLabel")
    label.Parent = parent
    label.Text = name
    label.Size = UDim2.new(0, 150, 0, 30)
    label.Position = UDim2.new(0, 0, 0, y)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.fromRGB(220, 190, 255)
    label.Font = Enum.Font.Gotham

    local bg = Instance.new("TextButton")
    bg.Text = ""
    bg.Parent = parent
    bg.Size = UDim2.new(0, 60, 0, 30)
    bg.Position = UDim2.new(1, -65, 0, y)
    bg.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    applyGradient(bg)
    Instance.new("UICorner", bg).CornerRadius = UDim.new(1, 0)

    local circle = Instance.new("Frame")
    circle.Parent = bg
    circle.Size = UDim2.new(0, 24, 0, 24)
    circle.Position = UDim2.new(0, 3, 0.5, -12)
    circle.BackgroundColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", circle).CornerRadius = UDim.new(1, 0)

    return bg, circle
end

-- ИСПРАВЛЕННАЯ ФУНКЦИЯ для переключения тоглов (убирает баг с пропажей кружка и белым фоном)
local function setToggleState(bg, circle, state, toggleName)
    TweenService:Create(circle, TweenInfo.new(.2), {Position = state and UDim2.new(1,-27,.5,-12) or UDim2.new(0,3,.5,-12)}):Play()
    if state then
        bg.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        applyGradient(bg)
    else
        for _, v in pairs(bg:GetChildren()) do
            if v:IsA("UIGradient") then v:Destroy() end
        end
        bg.BackgroundColor3 = Color3.fromRGB(50,50,60)
    end
    if toggleName then
        showNotification(toggleName .. (state and " включен!" or " выключен."))
    end
end

-- Хелпер для создания кнопок с градиентом (исправляет исчезновение текста)
local function createMenuBtn(parent, text, posY, showArrow)
    local btn = Instance.new("TextButton")
    btn.Parent = parent
    btn.Size = UDim2.new(1,-20,0,35)
    btn.Position = UDim2.new(0,10,0,posY)
    btn.BackgroundColor3 = Color3.fromRGB(255,255,255)
    btn.Text = "" -- Очищаем стандартный текст
    applyGradient(btn)
    Instance.new("UICorner", btn)
    
    local lbl = Instance.new("TextLabel", btn)
    lbl.Size = UDim2.new(1,0,1,0)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
    lbl.Font = Enum.Font.GothamBold
    
    local arrow = nil
    if showArrow then
        arrow = Instance.new("TextLabel", btn)
        arrow.Size = UDim2.new(0,20,1,0)
        arrow.Position = UDim2.new(1,-25,0,0)
        arrow.BackgroundTransparency = 1
        arrow.Text = "▼"
        arrow.TextColor3 = Color3.fromRGB(255, 255, 255)
    end
    
    return btn, arrow, lbl
end

-- === ПРИВЕТСТВЕННОЕ МЕНЮ (CHERRY TEAM & AMETHYSTGIRL) ===
local WelcomeGui = Instance.new("Frame")
WelcomeGui.Parent = ScreenGui
WelcomeGui.Size = UDim2.new(0, 440, 0, 200)
WelcomeGui.Position = UDim2.new(0.5, -220, 0.5, -100)
WelcomeGui.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
WelcomeGui.BackgroundTransparency = 0.15
WelcomeGui.BorderSizePixel = 0
applyGradient(WelcomeGui)

local WelcomeCorner = Instance.new("UICorner", WelcomeGui)
WelcomeCorner.CornerRadius = UDim.new(0, 12)

local WelcomeStroke = Instance.new("UIStroke", WelcomeGui)
WelcomeStroke.Color = Color3.fromRGB(220, 190, 255)
WelcomeStroke.Thickness = 1.5

local WelcomeText = Instance.new("TextLabel", WelcomeGui)
WelcomeText.Size = UDim2.new(1, -40, 0, 70)
WelcomeText.Position = UDim2.new(0, 20, 0, 30)
WelcomeText.BackgroundTransparency = 1
WelcomeText.Text = "We are glad to see you sincerely Cherry Team and AmethystGirl"
WelcomeText.Font = Enum.Font.GothamBold
WelcomeText.TextSize = 16
WelcomeText.TextColor3 = Color3.fromRGB(255, 255, 255)
WelcomeText.TextWrapped = true
WelcomeText.TextAlignment = Enum.TextXAlignment.Center

-- Кнопка Our GitHub
local GithubBtn = Instance.new("TextButton", WelcomeGui)
GithubBtn.Size = UDim2.new(0, 180, 0, 40)
GithubBtn.Position = UDim2.new(0, 25, 0, 125)
GithubBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
GithubBtn.Text = ""
applyGradient(GithubBtn)
Instance.new("UICorner", GithubBtn).CornerRadius = UDim.new(0, 8)

local GithubLbl = Instance.new("TextLabel", GithubBtn)
GithubLbl.Size = UDim2.new(1, 0, 1, 0)
GithubLbl.BackgroundTransparency = 1
GithubLbl.Text = "our GitHub"
GithubLbl.Font = Enum.Font.GothamBold
GithubLbl.TextSize = 14
GithubLbl.TextColor3 = Color3.fromRGB(255, 255, 255)

GithubBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard("https://github.com/CherryCalCor")
    elseif toclipboard then
        toclipboard("https://github.com/CherryCalCor")
    end
end)

-- Кнопка Launching the main menu
local LaunchBtn = Instance.new("TextButton", WelcomeGui)
LaunchBtn.Size = UDim2.new(0, 190, 0, 40)
LaunchBtn.Position = UDim2.new(1, -215, 0, 125)
LaunchBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
LaunchBtn.Text = ""
applyGradient(LaunchBtn)
Instance.new("UICorner", LaunchBtn).CornerRadius = UDim.new(0, 8)

local LaunchLbl = Instance.new("TextLabel", LaunchBtn)
LaunchLbl.Size = UDim2.new(1, 0, 1, 0)
LaunchLbl.BackgroundTransparency = 1
LaunchLbl.Text = "Launching the main menu"
LaunchLbl.Font = Enum.Font.GothamBold
LaunchLbl.TextSize = 13
LaunchLbl.TextColor3 = Color3.fromRGB(255, 255, 255)

local welcomeDone = false
LaunchBtn.MouseButton1Click:Connect(function()
    welcomeDone = true
    WelcomeGui:Destroy()
end)

makeDraggable(WelcomeGui)

-- Цикл задерживает выполнение скрипта, пока не нажата кнопка Launch
repeat task.wait() until welcomeDone


do
    local LoadingFrame = Instance.new("Frame")
    local LoadingTitle = Instance.new("TextLabel")
    local LoadingBarBG = Instance.new("Frame")
    local LoadingBar = Instance.new("Frame")
    local LoadingCorner1 = Instance.new("UICorner")
    local LoadingCorner2 = Instance.new("UICorner")

    LoadingFrame.Parent = ScreenGui
    LoadingFrame.Size = UDim2.new(0,500,0,300)
    LoadingFrame.Position = UDim2.new(0.5,-250,0.5,-150)
    LoadingFrame.BackgroundColor3 = Color3.fromRGB(255,255,255)
    applyGradient(LoadingFrame)
    LoadingFrame.BackgroundTransparency = 0.15
    LoadingFrame.BorderSizePixel = 0
    Instance.new("UICorner", LoadingFrame).CornerRadius = UDim.new(0,20)
    LoadingFrame.ZIndex = 999

    local LoadingStroke = Instance.new("UIStroke", LoadingFrame)
    LoadingStroke.Color = Color3.fromRGB(220,190,255)
    LoadingStroke.Thickness = 1.5

    LoadingTitle.Parent = LoadingFrame
    LoadingTitle.Size = UDim2.new(1,0,0,60)
    LoadingTitle.Position = UDim2.new(0,0,0.4,0)
    LoadingTitle.BackgroundTransparency = 1
    LoadingTitle.Text = "SEPTUS HUB"
    LoadingTitle.Font = Enum.Font.GothamBlack
    LoadingTitle.TextSize = 34
    LoadingTitle.TextColor3 = Color3.fromRGB(220,190,255)
    LoadingTitle.ZIndex = 1000

    local LoadingStar = Instance.new("TextLabel", LoadingFrame)
    LoadingStar.Size = UDim2.new(0,80,0,80)
    LoadingStar.Position = UDim2.new(0.5,-40,0.3,-40)
    LoadingStar.BackgroundTransparency = 1
    LoadingStar.Text = "★"
    LoadingStar.TextSize = 65
    LoadingStar.Font = Enum.Font.SourceSansBold
    LoadingStar.TextColor3 = Color3.fromRGB(220,190,255)
    LoadingStar.Rotation = 0
    LoadingStar.ZIndex = 1000

    local StarStroke = Instance.new("UIStroke", LoadingStar)
    StarStroke.Color = Color3.fromRGB(235,215,255)
    StarStroke.Thickness = 1.5

    task.spawn(function()
        while LoadingStar.Parent do
            local rotate = TweenService:Create(LoadingStar, TweenInfo.new(2, Enum.EasingStyle.Linear), {Rotation = LoadingStar.Rotation + 360})
            rotate:Play()
            rotate.Completed:Wait()
        end
    end)

    task.spawn(function()
        while LoadingStar.Parent do
            TweenService:Create(LoadingStar, TweenInfo.new(1), {TextTransparency = 0.2}):Play()
            task.wait(1)
            TweenService:Create(LoadingStar, TweenInfo.new(1), {TextTransparency = 0}):Play()
            task.wait(1)
        end
    end)

    LoadingBarBG.Parent = LoadingFrame
    LoadingBarBG.Size = UDim2.new(0,250,0,10)
    LoadingBarBG.Position = UDim2.new(0.5,-125,0.55,0)
    LoadingBarBG.BackgroundColor3 = Color3.fromRGB(40,40,50)
    LoadingBarBG.BorderSizePixel = 0
    LoadingBarBG.ZIndex = 1000
    LoadingCorner1.Parent = LoadingBarBG

    LoadingBar.Parent = LoadingBarBG
    LoadingBar.Size = UDim2.new(0,0,1,0)
    LoadingBar.BackgroundColor3 = Color3.fromRGB(210,180,255)
    LoadingBar.BorderSizePixel = 0
    LoadingBar.ZIndex = 1001
    LoadingCorner2.Parent = LoadingBar

    TweenService:Create(LoadingBar, TweenInfo.new(7, Enum.EasingStyle.Linear), {Size = UDim2.new(1,0,1,0)}):Play()

    task.wait(7)

    TweenService:Create(LoadingFrame, TweenInfo.new(0.8), {BackgroundTransparency = 1}):Play()
    TweenService:Create(LoadingTitle, TweenInfo.new(0.8), {TextTransparency = 1}):Play()
    TweenService:Create(LoadingBarBG, TweenInfo.new(0.8), {BackgroundTransparency = 1}):Play()
    TweenService:Create(LoadingBar, TweenInfo.new(0.8), {BackgroundTransparency = 1}):Play()
    task.wait(1)
    TweenService:Create(LoadingStar, TweenInfo.new(0.8), {TextTransparency = 1, Rotation = LoadingStar.Rotation + 90}):Play()
    task.wait(0.8)

    task.wait(0.8)
    LoadingFrame:Destroy()
    _G.MainMenuLoaded = true -- Сигнализируем, что основное меню готово
end

Frame.Visible = true

Corner.Parent = Frame
Corner.CornerRadius = UDim.new(0,10)

Stroke.Parent = Frame
Stroke.Color = Color3.fromRGB(210,180,255)
Stroke.Thickness = 1.2
Stroke.Transparency = 0

TopBar.Parent = Frame
TopBar.Size = UDim2.new(1, -2, 0, 1)
TopBar.Position = UDim2.new(0, 1, 0.12, 0)
TopBar.BackgroundColor3 = Color3.fromRGB(140,140,140)
TopBar.BackgroundTransparency = 0.35
TopBar.BorderSizePixel = 0

local SideBar = Instance.new("Frame", Frame)
SideBar.Size = UDim2.new(0, 1, 1, -30)
SideBar.Position = UDim2.new(0, 100, 0, 30)
SideBar.BackgroundColor3 = Color3.fromRGB(140,140,140)
SideBar.BackgroundTransparency = 0.35
SideBar.BorderSizePixel = 0

local function createNavBtn(text)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0,80,0,28)
    btn.BackgroundColor3 = Color3.fromRGB(255,255,255)
    applyGradient(btn)
    btn.Text = "" -- Убираем стандартный текст
    btn.BorderSizePixel = 0
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)
    
    local lbl = Instance.new("TextLabel", btn)
    lbl.Size = UDim2.new(1,0,1,0)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 14

    local indicator = Instance.new("Frame")
    indicator.Name = "Indicator"
    indicator.Size = UDim2.new(0, 3, 0.6, 0)
    indicator.Position = UDim2.new(1, -6, 0.2, 0)
    indicator.BackgroundColor3 = Color3.fromRGB(220, 190, 255)
    indicator.BorderSizePixel = 0
    indicator.BackgroundTransparency = 1
    Instance.new("UICorner", indicator).CornerRadius = UDim.new(1, 0)
    indicator.Parent = btn
    
    return btn
end

local HomeBtn = createNavBtn("Home")
local ServerBtn = createNavBtn("Server")
local VisualsBtn = createNavBtn("Visuals")
local TeleportBtn = createNavBtn("Teleport")
local MovedBtn = createNavBtn("Moved")
local MainMiscBtn = createNavBtn("Misc")

local SideMenuScroll = Instance.new("ScrollingFrame", Frame)
SideMenuScroll.Size = UDim2.new(0, 105, 1, -35)
SideMenuScroll.Position = UDim2.new(0, 0, 0, 35)
SideMenuScroll.BackgroundTransparency = 1
SideMenuScroll.ScrollBarThickness = 2
SideMenuScroll.ScrollBarImageColor3 = Color3.fromRGB(220,190,255)
SideMenuScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
SideMenuScroll.BorderSizePixel = 0
SideMenuScroll.ClipsDescendants = true

local SideLayout = Instance.new("UIListLayout", SideMenuScroll)
SideLayout.Padding = UDim.new(0, 7)
SideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
SideLayout.SortOrder = Enum.SortOrder.LayoutOrder

HomeBtn.LayoutOrder = 1
ServerBtn.LayoutOrder = 2
VisualsBtn.LayoutOrder = 3
TeleportBtn.LayoutOrder = 4
MovedBtn.LayoutOrder = 5
MainMiscBtn.LayoutOrder = 6

for _, btn in pairs({HomeBtn, ServerBtn, VisualsBtn, TeleportBtn, MovedBtn, MainMiscBtn}) do
    btn.Parent = SideMenuScroll
end

SideLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    SideMenuScroll.CanvasSize = UDim2.new(0, 0, 0, SideLayout.AbsoluteContentSize.Y + 10)
end)

local function createPage(canvasY)
    local page = Instance.new("ScrollingFrame", Frame)
    page.Position = UDim2.new(0,110,0,40)
    page.Size = UDim2.new(1,-120,1,-50)
    page.CanvasSize = UDim2.new(0,0,0,canvasY)
    page.ScrollBarThickness = 4
    page.BackgroundTransparency = 1
    page.Visible = false
    page.BorderSizePixel = 0
    return page
end

local HomePage = createPage(400)
local VisualsPage = createPage(900)
local TeleportPage = createPage(700)
local MovedPage = createPage(500)
local MainMiscPage = createPage(500)
local SettingsPage = createPage(600)

-- Создание кнопки навигации
local SettingsBtn = createNavBtn("Settings")
SettingsBtn.LayoutOrder = 7
SettingsBtn.Parent = SideMenuScroll


local ServerPage = Instance.new("Frame", Frame)
ServerPage.Visible = false
ServerPage.Position = UDim2.new(0,110,0,40)
ServerPage.Size = UDim2.new(1,-120,1,-50)
ServerPage.BackgroundTransparency = 1

do
    local SliderBG = Instance.new("Frame")
    SliderBG.Size = UDim2.new(1,-20,0,8)
    SliderBG.BackgroundColor3 = Color3.fromRGB(50,50,60)
    SliderBG.BorderSizePixel = 0
    Instance.new("UICorner", SliderBG)

    local SpeedLabel = Instance.new("TextLabel", MovedPage)
    SpeedLabel.Size = UDim2.new(1,0,0,25)
    SpeedLabel.BackgroundTransparency = 1
    SpeedLabel.Text = "Speed: 16"
    SpeedLabel.TextColor3 = Color3.fromRGB(220, 190, 255)

    local SpeedSlider = SliderBG:Clone()
    SpeedSlider.Parent = MovedPage
    SpeedSlider.Position = UDim2.new(0, 10, 0, 35)

    local SpeedFill = Instance.new("Frame", SpeedSlider)
    SpeedFill.Size = UDim2.new(0,0,1,0)
    SpeedFill.BackgroundColor3 = Color3.fromRGB(220,190,255)
    SpeedFill.BorderSizePixel = 0
    Instance.new("UICorner", SpeedFill)

    local SpeedKnob = Instance.new("Frame", SpeedSlider)
    SpeedKnob.Size = UDim2.new(0, 16, 0, 16)
    SpeedKnob.Position = UDim2.new(0, -8, 0.5, -8)
    SpeedKnob.BackgroundColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", SpeedKnob).CornerRadius = UDim.new(1, 0)

    local speed = 16
    local speedDragging = false

    local function setSpeed(x)
        local percent = math.clamp((x - SpeedSlider.AbsolutePosition.X) / SpeedSlider.AbsoluteSize.X, 0, 1)
        speed = math.floor(16 + (200 - 16) * percent)
        SpeedLabel.Text = "Speed: " .. speed
        SpeedFill.Size = UDim2.new(percent, 0, 1, 0)
        SpeedKnob.Position = UDim2.new(percent, -8, 0.5, -8)
        
        local char = game.Players.LocalPlayer.Character
        if char then
            local hum = char:FindFirstChild("Humanoid")
            if hum then hum.WalkSpeed = speed end
        end
    end

    SpeedSlider.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then
            speedDragging = true
            setSpeed(i.Position.X)
        end
    end)

    UIS.InputChanged:Connect(function(i)
        if speedDragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            setSpeed(i.Position.X)
        end
    end)

    UIS.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then speedDragging = false end
    end)

    local JumpLabel = Instance.new("TextLabel", MovedPage)
    JumpLabel.Position = UDim2.new(0, 0, 0, 80)
    JumpLabel.Size = UDim2.new(1, 0, 0, 25)
    JumpLabel.Text = "Jump: 50"
    JumpLabel.BackgroundTransparency = 1
    JumpLabel.TextColor3 = Color3.fromRGB(220, 190, 255)
    
    local JumpSlider = SliderBG:Clone()
    JumpSlider.Parent = MovedPage
    JumpSlider.Position = UDim2.new(0, 10, 0, 115)

    local JumpFill = Instance.new("Frame", JumpSlider)
    JumpFill.Size = UDim2.new(0, 0, 1, 0)
    JumpFill.BackgroundColor3 = Color3.fromRGB(220,190,255)
    JumpFill.BorderSizePixel = 0
    Instance.new("UICorner", JumpFill)

    local JumpKnob = Instance.new("Frame", JumpSlider)
    JumpKnob.Size = UDim2.new(0, 16, 0, 16)
    JumpKnob.Position = UDim2.new(0, -8, 0.5, -8)
    JumpKnob.BackgroundColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", JumpKnob).CornerRadius = UDim.new(1, 0)

    local jump = 50
    local jumpDragging = false

    local function setJump(x)
        local percent = math.clamp((x - JumpSlider.AbsolutePosition.X) / JumpSlider.AbsoluteSize.X, 0, 1)
        jump = math.floor(50 + (200 - 50) * percent)
        JumpLabel.Text = "Jump: " .. jump
        JumpFill.Size = UDim2.new(percent, 0, 1, 0)
        JumpKnob.Position = UDim2.new(percent, -8, 0.5, -8)
        
        local char = game.Players.LocalPlayer.Character
        if char then
            local hum = char:FindFirstChild("Humanoid")
            if hum then
                hum.UseJumpPower = true
                hum.JumpPower = jump
            end
        end
    end

    JumpSlider.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then
            jumpDragging = true
            setJump(i.Position.X)
        end
    end)

    UIS.InputChanged:Connect(function(i)
        if jumpDragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            setJump(i.Position.X)
        end
    end)

    UIS.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then jumpDragging = false end
    end)

    local EJToggle, EJCircle = createToggle(MovedPage, "Endless Jump", 170)
    local endless = false

    EJToggle.MouseButton1Click:Connect(function()
        endless = not endless
        setToggleState(EJToggle, EJCircle, endless, "Endless Jump")
    end)

    UIS.JumpRequest:Connect(function()
        if endless and game.Players.LocalPlayer.Character then
            local hum = game.Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
        end
    end)

    local WallToggle, WallCircle = createToggle(MovedPage, "Climbing Walls", 220)
    local wall = false

    WallToggle.MouseButton1Click:Connect(function()
        wall = not wall
        setToggleState(WallToggle, WallCircle, wall, "Climbing Walls")
    end)

    task.spawn(function()
        while task.wait() do
            if wall and game.Players.LocalPlayer.Character then
                local hum = game.Players.LocalPlayer.Character:FindFirstChild("Humanoid")
                if hum then hum:ChangeState(Enum.HumanoidStateType.Climbing) end
            end
        end
    end)
end

do
    local DiffBtn, DiffArrow = createMenuBtn(MainMiscPage, "Different", 10, true)
    
    local DiffMenu = Instance.new("Frame", MainMiscPage)
    DiffMenu.Visible = false
    DiffMenu.Position = UDim2.new(0,10,0,50)
    DiffMenu.BackgroundTransparency = 1
    DiffMenu.ClipsDescendants = true
    DiffMenu.Size = UDim2.new(1,-20,0,0)

    local OtherBtn, OtherArrow = createMenuBtn(MainMiscPage, "Other", 55, true)
    
    local OtherMenu = Instance.new("Frame", MainMiscPage)
    OtherMenu.Visible = false
    OtherMenu.Position = UDim2.new(0,10,0,95)
    OtherMenu.BackgroundTransparency = 1
    OtherMenu.ClipsDescendants = true
    OtherMenu.Size = UDim2.new(1,-20,0,0)

    local diffOpened = false
    local otherOpened = false

    local function updateMainMiscPositions()
        local diffHeight = diffOpened and 160 or 0
        TweenService:Create(OtherBtn, TweenInfo.new(.2), {Position = UDim2.new(0,10,0, 55 + diffHeight)}):Play()
        TweenService:Create(OtherMenu, TweenInfo.new(.2), {Position = UDim2.new(0,10,0, 95 + diffHeight)}):Play()
    end

    DiffBtn.MouseButton1Click:Connect(function()
        diffOpened = not diffOpened
        TweenService:Create(DiffArrow, TweenInfo.new(.2), {Rotation = diffOpened and 180 or 0}):Play()
        DiffArrow.Text = diffOpened and "▲" or "▼"
        DiffMenu.Visible = true
        TweenService:Create(DiffMenu, TweenInfo.new(.2), {Size = diffOpened and UDim2.new(1,-20,0,160) or UDim2.new(1,-20,0,0)}):Play()
        task.delay(.2, function() if not diffOpened then DiffMenu.Visible = false end end)
        updateMainMiscPositions()
    end)

    OtherBtn.MouseButton1Click:Connect(function()
        otherOpened = not otherOpened
        TweenService:Create(OtherArrow, TweenInfo.new(.2), {Rotation = otherOpened and 180 or 0}):Play()
        OtherArrow.Text = otherOpened and "▲" or "▼"
        OtherMenu.Visible = true
        TweenService:Create(OtherMenu, TweenInfo.new(.2), {Size = otherOpened and UDim2.new(1,-20,0,80) or UDim2.new(1,-20,0,0)}):Play()
        task.delay(.2, function() if not otherOpened then OtherMenu.Visible = false end end)
    end)

    local AntiAFKToggle, AntiAFKCircle = createToggle(DiffMenu, "Anti-AFK", 0)
    local NoclipToggle, NoclipCircle = createToggle(DiffMenu, "Noclip", 40)
    local AutoReconToggle, AutoReconCircle = createToggle(DiffMenu, "Auto Reconnect", 80)
    local FreeCamToggle, FreeCamCircle = createToggle(DiffMenu, "Free camera", 120)

    local antiAfkEnabled = false
    local noclipEnabled = false
    local autoReconEnabled = false
    local freeCamEnabled = false

    AntiAFKToggle.MouseButton1Click:Connect(function()
        antiAfkEnabled = not antiAfkEnabled
        setToggleState(AntiAFKToggle, AntiAFKCircle, antiAfkEnabled, "Anti-AFK")
    end)

    NoclipToggle.MouseButton1Click:Connect(function()
        noclipEnabled = not noclipEnabled
        setToggleState(NoclipToggle, NoclipCircle, noclipEnabled, "Noclip")
    end)

    AutoReconToggle.MouseButton1Click:Connect(function()
        autoReconEnabled = not autoReconEnabled
        setToggleState(AutoReconToggle, AutoReconCircle, autoReconEnabled, "Auto Reconnect")
    end)

    game:GetService("Players").LocalPlayer.Idled:Connect(function()
        if antiAfkEnabled then
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end
    end)

    RunService.Stepped:Connect(function()
        if noclipEnabled and game.Players.LocalPlayer.Character then
            for _, part in pairs(game.Players.LocalPlayer.Character:GetDescendants()) do
                if part:IsA("BasePart") and part.CanCollide then
                    part.CanCollide = false
                end
            end
        end
    end)

    game:GetService("GuiService").ErrorMessageChanged:Connect(function()
        if autoReconEnabled then
            task.wait(0.5)
            TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, game.Players.LocalPlayer)
        end
    end)

    local cam = workspace.CurrentCamera
    local fcSpeed = 2
    local fcKeys = {W = false, A = false, S = false, D = false, Space = false, LeftShift = false}

    FreeCamToggle.MouseButton1Click:Connect(function()
        freeCamEnabled = not freeCamEnabled
        setToggleState(FreeCamToggle, FreeCamCircle, freeCamEnabled, "Free camera")
        
        if freeCamEnabled then
            cam.CameraType = Enum.CameraType.Scriptable
            if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = true
            end
        else
            cam.CameraType = Enum.CameraType.Custom
            if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
            end
        end
    end)

    UIS.InputBegan:Connect(function(input, gpe)
        if gpe then return end
        if input.KeyCode == Enum.KeyCode.W then fcKeys.W = true
        elseif input.KeyCode == Enum.KeyCode.A then fcKeys.A = true
        elseif input.KeyCode == Enum.KeyCode.S then fcKeys.S = true
        elseif input.KeyCode == Enum.KeyCode.D then fcKeys.D = true
        elseif input.KeyCode == Enum.KeyCode.Space then fcKeys.Space = true
        elseif input.KeyCode == Enum.KeyCode.LeftShift then fcKeys.LeftShift = true end
    end)

    UIS.InputEnded:Connect(function(input, gpe)
        if input.KeyCode == Enum.KeyCode.W then fcKeys.W = false
        elseif input.KeyCode == Enum.KeyCode.A then fcKeys.A = false
        elseif input.KeyCode == Enum.KeyCode.S then fcKeys.S = false
        elseif input.KeyCode == Enum.KeyCode.D then fcKeys.D = false
        elseif input.KeyCode == Enum.KeyCode.Space then fcKeys.Space = false
        elseif input.KeyCode == Enum.KeyCode.LeftShift then fcKeys.LeftShift = false end
    end)

    RunService.RenderStepped:Connect(function()
        if freeCamEnabled then
            local moveVector = Vector3.new()
            if fcKeys.W then moveVector = moveVector + Vector3.new(0, 0, -1) end
            if fcKeys.S then moveVector = moveVector + Vector3.new(0, 0, 1) end
            if fcKeys.A then moveVector = moveVector + Vector3.new(-1, 0, 0) end
            if fcKeys.D then moveVector = moveVector + Vector3.new(1, 0, 0) end
            if fcKeys.Space then moveVector = moveVector + Vector3.new(0, 1, 0) end
            if fcKeys.LeftShift then moveVector = moveVector + Vector3.new(0, -1, 0) end
            cam.CFrame = cam.CFrame * CFrame.new(moveVector * fcSpeed)
        end
    end)

    local function createFloatingMenu(titleText)
        local win = Instance.new("Frame", ScreenGui)
        win.Size = UDim2.new(0, 200, 0, 250)
        win.Position = UDim2.new(0.5, -100, 0.5, -125)
        win.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        applyGradient(win)
        win.BackgroundTransparency = 0.1
        win.Visible = false
        win.ClipsDescendants = true
        Instance.new("UICorner", win).CornerRadius = UDim.new(0, 10)
        
        local stroke = Instance.new("UIStroke", win)
        stroke.Color = Color3.fromRGB(210, 180, 255)
        stroke.Thickness = 1.2

        local topBar = Instance.new("Frame", win)
        topBar.Size = UDim2.new(1, 0, 0, 30)
        topBar.BackgroundTransparency = 1
        topBar.Active = true
        makeDraggable(win, topBar)

        local title = Instance.new("TextLabel", topBar)
        title.Size = UDim2.new(0, 120, 1, 0)
        title.Position = UDim2.new(0, 15, 0, 0)
        title.BackgroundTransparency = 1
        title.Text = titleText
        title.TextColor3 = Color3.fromRGB(255, 255, 255)
        title.Font = Enum.Font.GothamBold
        title.TextXAlignment = Enum.TextXAlignment.Left

        local minBtn = Instance.new("TextButton", topBar)
        minBtn.Size = UDim2.new(0, 25, 0, 25)
        minBtn.Position = UDim2.new(1, -60, 0, 2)
        minBtn.BackgroundTransparency = 1
        minBtn.Text = "-"
        minBtn.TextColor3 = Color3.new(1,1,1)
        minBtn.TextSize = 18

        local closeBtn = Instance.new("TextButton", topBar)
        closeBtn.Size = UDim2.new(0, 25, 0, 25)
        closeBtn.Position = UDim2.new(1, -30, 0, 2)
        closeBtn.BackgroundTransparency = 1
        closeBtn.Text = "X"
        closeBtn.TextColor3 = Color3.new(1,1,1)
        closeBtn.Font = Enum.Font.GothamBold

        local scroll = Instance.new("ScrollingFrame", win)
        scroll.Size = UDim2.new(1, -10, 1, -80)
        scroll.Position = UDim2.new(0, 5, 0, 35)
        scroll.BackgroundTransparency = 1
        scroll.ScrollBarThickness = 2
        scroll.BorderSizePixel = 0
        
        local layout = Instance.new("UIListLayout", scroll)
        layout.Padding = UDim.new(0, 5)

        local actionBtn = Instance.new("TextButton", win)
        actionBtn.Size = UDim2.new(1, -20, 0, 30)
        actionBtn.Position = UDim2.new(0, 10, 0, 212) 
        actionBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        applyGradient(actionBtn)
        actionBtn.Text = "" 
        Instance.new("UICorner", actionBtn).CornerRadius = UDim.new(0, 6)

        local actionLbl = Instance.new("TextLabel", actionBtn)
        actionLbl.Size = UDim2.new(1, 0, 1, 0)
        actionLbl.BackgroundTransparency = 1
        actionLbl.Text = titleText .. ": [None]"
        actionLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
        actionLbl.Font = Enum.Font.GothamBold
        actionLbl.TextSize = 14

        local isMin = false
        minBtn.MouseButton1Click:Connect(function()
            isMin = not isMin
            if isMin then
                win:TweenSize(UDim2.new(0, 200, 0, 30), "Out", "Quad", 0.2, true)
            else
                win:TweenSize(UDim2.new(0, 200, 0, 250), "Out", "Quad", 0.2, true)
            end
        end)

        return win, closeBtn, scroll, layout, actionBtn, actionLbl
    end

    local function populatePlayers(scroll, layout, actionLbl, defaultText, targetCallback)
        for _, v in pairs(scroll:GetChildren()) do
            if v:IsA("TextButton") then v:Destroy() end
        end
        
        for _, plr in ipairs(game.Players:GetPlayers()) do
            if plr ~= game.Players.LocalPlayer then
                local btn = Instance.new("TextButton")
                btn.Parent = scroll
                btn.Size = UDim2.new(1, -5, 0, 25)
                btn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
                btn.TextColor3 = Color3.new(1, 1, 1)
                btn.Font = Enum.Font.Gotham
                btn.TextSize = 13
                btn.Text = plr.Name
                Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)

                btn.MouseButton1Click:Connect(function()
                    targetCallback(plr)
                    actionLbl.Text = defaultText .. ": " .. plr.Name
                end)
            end
        end
        
        task.delay(0.05, function()
            scroll.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 5)
        end)
    end

    local FlingToggle, FlingCircle = createToggle(OtherMenu, "Fling", 0)
    local BringToggle, BringCircle = createToggle(OtherMenu, "Bring parts", 40)

    local flingEnabled = false
    local bringMenuEnabled = false

    local FlingWin, FlingClose, FlingScroll, FlingLayout, FlingActionBtn, FlingActionLbl = createFloatingMenu("Fling")
    local BringWin, BringClose, BringScroll, BringLayout, BringActionBtn, BringActionLbl = createFloatingMenu("Bring")

    local currentFlingTarget = nil
    local currentBringTarget = nil

    FlingToggle.MouseButton1Click:Connect(function()
        flingEnabled = not flingEnabled
        setToggleState(FlingToggle, FlingCircle, flingEnabled)
        FlingWin.Visible = flingEnabled
        if flingEnabled then
            populatePlayers(FlingScroll, FlingLayout, FlingActionLbl, "Fling", function(p) currentFlingTarget = p end)
        end
    end)

    BringToggle.MouseButton1Click:Connect(function()
        bringMenuEnabled = not bringMenuEnabled
        setToggleState(BringToggle, BringCircle, bringMenuEnabled)
        BringWin.Visible = bringMenuEnabled
        if bringMenuEnabled then
            populatePlayers(BringScroll, BringLayout, BringActionLbl, "Bring", function(p) currentBringTarget = p end)
        end
    end)

    FlingClose.MouseButton1Click:Connect(function()
        FlingWin.Visible = false
        flingEnabled = false
        setToggleState(FlingToggle, FlingCircle, false)
    end)

    BringClose.MouseButton1Click:Connect(function()
        BringWin.Visible = false
        bringMenuEnabled = false
        setToggleState(BringToggle, BringCircle, false)
    end)

    local isFlinging = false
    FlingActionBtn.MouseButton1Click:Connect(function()
        if not currentFlingTarget or not currentFlingTarget.Character or isFlinging then return end
        local char = game.Players.LocalPlayer.Character
        if not char or not char:FindFirstChild("HumanoidRootPart") then return end

        isFlinging = true
        showNotification("Fling применяется к " .. currentFlingTarget.Name)
        local hrp = char.HumanoidRootPart
        local oldPos = hrp.CFrame
        
        local bv = Instance.new("BodyAngularVelocity")
        bv.AngularVelocity = Vector3.new(0, 99999, 0)
        bv.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        bv.P = math.huge
        bv.Parent = hrp
        
        local bt = Instance.new("BodyThrust")
        bt.Force = Vector3.new(9999, 9999, 9999)
        bt.Parent = hrp
        
        char.Humanoid.Sit = true

        local flingTime = 0
        local flingCon
        flingCon = RunService.Stepped:Connect(function(_, dt)
            flingTime = flingTime + dt
            if flingTime > 2.5 or not currentFlingTarget.Character or not currentFlingTarget.Character:FindFirstChild("HumanoidRootPart") then
                if bv then bv:Destroy() end
                if bt then bt:Destroy() end
                hrp.Velocity = Vector3.new()
                hrp.RotVelocity = Vector3.new()
                hrp.CFrame = oldPos
                char.Humanoid.Sit = false
                isFlinging = false
                flingCon:Disconnect()
                return
            end
            hrp.CFrame = currentFlingTarget.Character.HumanoidRootPart.CFrame
        end)
    end)

    BringActionBtn.MouseButton1Click:Connect(function()
        if not currentBringTarget or not currentBringTarget.Character then return end
        local targetHRP = currentBringTarget.Character:FindFirstChild("HumanoidRootPart")
        if not targetHRP then return end
        showNotification("Перемещение объектов к " .. currentBringTarget.Name)

        pcall(function() sethiddenproperty(game.Players.LocalPlayer, "MaximumSimulationRadius", math.huge) end)
        pcall(function() sethiddenproperty(game.Players.LocalPlayer, "SimulationRadius", math.huge) end)

        for _, part in pairs(workspace:GetDescendants()) do
            if part:IsA("BasePart") and not part.Anchored and not part.Parent:FindFirstChildOfClass("Humanoid") and not part.Parent:IsA("Accessory") then
                part.CFrame = targetHRP.CFrame
                part.Velocity = Vector3.new(0, 0, 0)
                part.RotVelocity = Vector3.new(0, 0, 0)
            end
        end
    end)
end

do
    local TPCard = Instance.new("Frame", TeleportPage)
    TPCard.Size = UDim2.new(1, -10, 0, 90)
    TPCard.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    applyGradient(TPCard)
    TPCard.BackgroundTransparency = .15
    Instance.new("UICorner", TPCard)

    local SelectedAvatar = Instance.new("ImageLabel", TPCard)
    SelectedAvatar.Size = UDim2.new(0, 60, 0, 60)
    SelectedAvatar.Position = UDim2.new(0, 10, 0.5, -30)
    SelectedAvatar.BackgroundTransparency = 1

    local SelectedName = Instance.new("TextLabel", TPCard)
    SelectedName.Position = UDim2.new(0, 80, 0, 15)
    SelectedName.Size = UDim2.new(1, -90, 0, 20)
    SelectedName.BackgroundTransparency = 1
    SelectedName.Text = "Player"
    SelectedName.Font = Enum.Font.GothamBold
    SelectedName.TextColor3 = Color3.new(1, 1, 1)

    local TPPlayer = nil

    local function makeTPBtn(text, x)
        local b = Instance.new("TextButton", TPCard)
        b.Size = UDim2.new(0, 100, 0, 30)
        b.Position = UDim2.new(0, x, 0, 50)
        b.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        b.Text = "" 
        applyGradient(b)
        Instance.new("UICorner", b)
        
        local lbl = Instance.new("TextLabel", b)
        lbl.Size = UDim2.new(1,0,1,0)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
        lbl.Font = Enum.Font.GothamBold
        lbl.TextSize = 14
        return b
    end

    local TPBtn = makeTPBtn("Teleport", 80)
    local SpectateBtn = makeTPBtn("Spectate", 190)

    local PlayersBtn, _, PlayersLbl = createMenuBtn(TeleportPage, "Players", 110, false)
    local ToolBtn, _, ToolLbl = createMenuBtn(TeleportPage, "Teleport Tool", 160, false)

    local PlayersMenu = Instance.new("ScrollingFrame", TeleportPage)
    PlayersMenu.Position = UDim2.new(0, 10, 0, 150)
    PlayersMenu.Size = UDim2.new(1, -20, 0, 0)
    PlayersMenu.Visible = false
    PlayersMenu.BackgroundTransparency = 1

    local playersOpen = false

    local function refreshPlayers()
        PlayersMenu:ClearAllChildren()
        local layout = Instance.new("UIListLayout")
        layout.Parent = PlayersMenu
        layout.Padding = UDim.new(0, 5)

        for _, plr in ipairs(game.Players:GetPlayers()) do
            if plr ~= game.Players.LocalPlayer then
                local btn = Instance.new("TextButton", PlayersMenu)
                btn.Size = UDim2.new(1, 0, 0, 35)
                btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                btn.Text = ""
                applyGradient(btn)
                Instance.new("UICorner", btn)

                local lbl = Instance.new("TextLabel", btn)
                lbl.Size = UDim2.new(1,0,1,0)
                lbl.BackgroundTransparency = 1
                lbl.Text = plr.Name
                lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
                lbl.Font = Enum.Font.GothamBold

                btn.MouseButton1Click:Connect(function()
                    TPPlayer = plr
                    SelectedName.Text = plr.Name
                    pcall(function()
                        SelectedAvatar.Image = game.Players:GetUserThumbnailAsync(plr.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
                    end)
                end)
            end
        end
        PlayersMenu.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y)
    end

    ToolBtn.MouseButton1Click:Connect(function()
        showNotification("Teleport Tool выдан!")
        local tool = Instance.new("Tool")
        tool.Name = "Teleport Tool"
        tool.RequiresHandle = false
        tool.Parent = game.Players.LocalPlayer.Backpack
        tool.Activated:Connect(function()
            local mouse = game.Players.LocalPlayer:GetMouse()
            if mouse.Hit and game.Players.LocalPlayer.Character then
                game.Players.LocalPlayer.Character:MoveTo(mouse.Hit.Position)
            end
        end)
    end)

    PlayersBtn.MouseButton1Click:Connect(function()
        playersOpen = not playersOpen
        refreshPlayers()
        PlayersMenu.Visible = true
        local menuHeight = playersOpen and 200 or 0
        TweenService:Create(PlayersMenu, TweenInfo.new(0.2), {Size = UDim2.new(1, -20, 0, menuHeight)}):Play()
        TweenService:Create(ToolBtn, TweenInfo.new(0.2), {Position = UDim2.new(0, 10, 0, 160 + menuHeight)}):Play()
    end)

    TPBtn.MouseButton1Click:Connect(function()
        if not TPPlayer then return end
        local my = game.Players.LocalPlayer.Character
        local target = TPPlayer.Character
        if my and target and target:FindFirstChild("HumanoidRootPart") then
            my:MoveTo(target.HumanoidRootPart.Position)
            showNotification("Телепортирован к " .. TPPlayer.Name)
        end
    end)

    SpectateBtn.MouseButton1Click:Connect(function()
        if not TPPlayer or not TPPlayer.Character then return end
        local hum = TPPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then 
            workspace.CurrentCamera.CameraSubject = hum
            showNotification("Наблюдение за " .. TPPlayer.Name)
        end
    end)
end

do
    local ESPBtn, Arrow = createMenuBtn(VisualsPage, "ESP", 10, true)

    local ESPMenu = Instance.new("Frame", VisualsPage)
    ESPMenu.Visible = false
    ESPMenu.Position = UDim2.new(0,10,0,50)
    ESPMenu.BackgroundTransparency = 1
    ESPMenu.ClipsDescendants = true
    ESPMenu.Size = UDim2.new(1,-20,0,0)

    local BoostBtn, BoostArrow = createMenuBtn(VisualsPage, "Boostfps", 55, true)

    local BoostMenu = Instance.new("Frame", VisualsPage)
    BoostMenu.Visible = false
    BoostMenu.Position = UDim2.new(0,10,0,95)
    BoostMenu.BackgroundTransparency = 1
    BoostMenu.ClipsDescendants = true
    BoostMenu.Size = UDim2.new(1,-20,0,0)

    local MiscBtn, MiscArrow = createMenuBtn(VisualsPage, "Misc", 95, true)

    local MiscMenu = Instance.new("Frame", VisualsPage)
    MiscMenu.Visible = false
    MiscMenu.Position = UDim2.new(0,10,0,135)
    MiscMenu.BackgroundTransparency = 1
    MiscMenu.ClipsDescendants = true
    MiscMenu.Size = UDim2.new(1,-20,0,0)

    local espOpened = false
    local boostOpened = false
    local miscOpened = false

    local HeadlessToggle, HeadlessCircle = createToggle(MiscMenu, "Headless", 55)
    local headless = false

    HeadlessToggle.MouseButton1Click:Connect(function()
        headless = not headless
        setToggleState(HeadlessToggle, HeadlessCircle, headless, "Headless")
        
        local char = game.Players.LocalPlayer.Character
        if char and char:FindFirstChild("Head") then
            char.Head.Transparency = headless and 1 or 0
            for _, v in pairs(char.Head:GetChildren()) do
               if v:IsA("Decal") then v.Transparency = headless and 1 or 0 end
            end
        end
    end)

    local KorbloxToggle, KorbloxCircle = createToggle(MiscMenu, "Korblox", 100)
    local korblox = false

    KorbloxToggle.MouseButton1Click:Connect(function()
        korblox = not korblox
        setToggleState(KorbloxToggle, KorbloxCircle, korblox, "Korblox")
        
        local char = game.Players.LocalPlayer.Character
        if char then
            local leg = char:FindFirstChild("Right Leg") or char:FindFirstChild("RightLowerLeg")
            if leg then leg.Transparency = korblox and 1 or 0 end
        end
    end)

    local function updatePositions()
        local espHeight = espOpened and 170 or 0
        local boostHeight = boostOpened and 260 or 0

        TweenService:Create(BoostBtn, TweenInfo.new(.2), {Position = UDim2.new(0,10,0, 55 + espHeight)}):Play()
        TweenService:Create(BoostMenu, TweenInfo.new(.2), {Position = UDim2.new(0,10,0, 95 + espHeight)}):Play()
        TweenService:Create(MiscBtn, TweenInfo.new(.2), {Position = UDim2.new(0,10,0, 95 + espHeight + boostHeight)}):Play()
        TweenService:Create(MiscMenu, TweenInfo.new(.2), {Position = UDim2.new(0,10,0, 135 + espHeight + boostHeight)}):Play()
    end

    MiscBtn.MouseButton1Click:Connect(function()
        miscOpened = not miscOpened
        TweenService:Create(MiscArrow, TweenInfo.new(.2), {Rotation = miscOpened and 180 or 0}):Play()
        MiscArrow.Text = miscOpened and "▲" or "▼"
        MiscMenu.Visible = true
        TweenService:Create(MiscMenu, TweenInfo.new(.2), {Size = miscOpened and UDim2.new(1,-20,0,200) or UDim2.new(1,-20,0,0)}):Play()
        task.delay(.2, function() if not miscOpened then MiscMenu.Visible = false end end)
    end)

    BoostBtn.MouseButton1Click:Connect(function()
        boostOpened = not boostOpened
        TweenService:Create(BoostArrow, TweenInfo.new(.2), {Rotation = boostOpened and 180 or 0}):Play()
        BoostArrow.Text = boostOpened and "▲" or "▼"
        BoostMenu.Visible = true
        TweenService:Create(BoostMenu, TweenInfo.new(.2), {Size = boostOpened and UDim2.new(1,-20,0,260) or UDim2.new(1,-20,0,0)}):Play()
        updatePositions()
    end)

    ESPBtn.MouseButton1Click:Connect(function()
        espOpened = not espOpened
        TweenService:Create(Arrow, TweenInfo.new(.2), {Rotation = espOpened and 180 or 0}):Play()
        Arrow.Text = espOpened and "▲" or "▼"
        ESPMenu.Visible = true
        TweenService:Create(ESPMenu, TweenInfo.new(.2), {Size = espOpened and UDim2.new(1,-20,0,160) or UDim2.new(1,-20,0,0)}):Play()
        task.delay(.2, function() if not espOpened then ESPMenu.Visible = false end end)
        updatePositions()
    end)

    local BoostToggle, BoostCircle = createToggle(BoostMenu, "Boostfps", 0)
    local SkinToggle, SkinCircle = createToggle(BoostMenu, "Skin Replacement", 40)
    local boostFPS = false
    local skinReplace = false

    BoostToggle.MouseButton1Click:Connect(function()
        boostFPS = not boostFPS
        setToggleState(BoostToggle, BoostCircle, boostFPS, "Boostfps")
        
        if boostFPS then
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("Texture") or obj:IsA("Decal") then obj.Transparency = 1 end
                if obj:IsA("BasePart") then obj.Material = Enum.Material.Plastic; obj.Reflectance = 0 end
            end
        end
    end)

    SkinToggle.MouseButton1Click:Connect(function()
        skinReplace = not skinReplace
        setToggleState(SkinToggle, SkinCircle, skinReplace, "Skin Replacement")
        
        for _, plr in pairs(game.Players:GetPlayers()) do
             if plr ~= game.Players.LocalPlayer and plr.Character then
                for _, v in pairs(plr.Character:GetDescendants()) do
                    if v:IsA("MeshPart") and skinReplace then v.MeshId = ""; v.Size = Vector3.new(1, 1, 1) end
                end
              end
         end
    end)

    local PotatoToggle, PotatoCircle = createToggle(BoostMenu, "Potato Mode", 80)
    local potatoEnabled = false

    PotatoToggle.MouseButton1Click:Connect(function()
        potatoEnabled = not potatoEnabled
        setToggleState(PotatoToggle, PotatoCircle, potatoEnabled, "Potato Mode")
        
        if potatoEnabled then
             for _, obj in pairs(workspace:GetDescendants()) do
                 if obj:IsA("Texture") or obj:IsA("Decal") then obj.Transparency = 1 end
                if obj:IsA("BasePart") then obj.Material = Enum.Material.SmoothPlastic; obj.Reflectance = 0; obj.CastShadow = false end
                if obj:IsA("MeshPart") then obj.TextureID = ""; obj.Material = Enum.Material.Plastic end
                if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire") then obj.Enabled = false end
            end
        end
    end)

    local RemoveToggle, RemoveCircle = createToggle(BoostMenu, "Removing Textures", 120)
    local removeEnabled = false
    local RemoveTool

    RemoveToggle.MouseButton1Click:Connect(function()
        removeEnabled = not removeEnabled
        setToggleState(RemoveToggle, RemoveCircle, removeEnabled, "Removing Textures")
        
        local player = game.Players.LocalPlayer
        if removeEnabled then
            RemoveTool = Instance.new("Tool")
            RemoveTool.Name = "Texture Remover"
            RemoveTool.RequiresHandle = false
            RemoveTool.Parent = player.Backpack
            RemoveTool.Activated:Connect(function()
                local mouse = player:GetMouse()
                local target = mouse.Target
                if target then
                     for _, v in pairs(target:GetDescendants()) do
                        if v:IsA("Texture") or v:IsA("Decal") then v:Destroy() end
                    end
                    if target:IsA("MeshPart") then target.TextureID = "" end
                end
             end)
        else
            if RemoveTool then RemoveTool:Destroy(); RemoveTool = nil end
        end
    end)

    local FullBrightToggle, FullBrightCircle = createToggle(BoostMenu, "FullBright", 160)
    local fullbright = false
    local oldLighting = {}

    FullBrightToggle.MouseButton1Click:Connect(function()
        fullbright = not fullbright
        setToggleState(FullBrightToggle, FullBrightCircle, fullbright, "FullBright")
         
        local Lighting = game:GetService("Lighting")
        if fullbright then
            oldLighting.Brightness = Lighting.Brightness
            oldLighting.ClockTime = Lighting.ClockTime
            oldLighting.GlobalShadows = Lighting.GlobalShadows
            oldLighting.FogEnd = Lighting.FogEnd
            Lighting.Brightness = 3; Lighting.ClockTime = 14; Lighting.GlobalShadows = false; Lighting.FogEnd = 999999
        else
            Lighting.Brightness = oldLighting.Brightness or 1
            Lighting.ClockTime = oldLighting.ClockTime or 14
            Lighting.GlobalShadows = oldLighting.GlobalShadows
            Lighting.FogEnd = oldLighting.FogEnd or 1000
        end
    end)

    local EffectsToggle, EffectsCircle = createToggle(BoostMenu, "Remove Effects", 200)
    local effectsEnabled = false

    EffectsToggle.MouseButton1Click:Connect(function()
        effectsEnabled = not effectsEnabled
        setToggleState(EffectsToggle, EffectsCircle, effectsEnabled, "Remove Effects")
        
        for _, v in pairs(game.Lighting:GetChildren()) do
            if v:IsA("BlurEffect") or v:IsA("BloomEffect") or v:IsA("ColorCorrectionEffect") or v:IsA("SunRaysEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("Atmosphere") then
                 v.Enabled = not effectsEnabled
            end
        end
    end)

    local NameToggle, NameCircle = createToggle(ESPMenu, "ESP Name", 0)
    local OutlineToggle, OutlineCircle = createToggle(ESPMenu, "ESP Outline", 40)
    local ItemToggle, ItemCircle = createToggle(ESPMenu, "Item ESP", 80)
    local nameESP = false
    local outlineESP = false
    local itemESP = false

    NameToggle.MouseButton1Click:Connect(function()
        nameESP = not nameESP
        setToggleState(NameToggle, NameCircle, nameESP, "ESP Name")
    end)

    OutlineToggle.MouseButton1Click:Connect(function()
        outlineESP = not outlineESP
        setToggleState(OutlineToggle, OutlineCircle, outlineESP, "ESP Outline")
    end)

    ItemToggle.MouseButton1Click:Connect(function()
        itemESP = not itemESP
        setToggleState(ItemToggle, ItemCircle, itemESP, "Item ESP")
    end)

    task.spawn(function()
        while task.wait(.5) do
            for _, plr in pairs(game.Players:GetPlayers()) do
                if plr ~= game.Players.LocalPlayer then
                     if itemESP then
                        for _, obj in pairs(workspace:GetDescendants()) do
                            if (obj:IsA("Tool") or obj.Name:lower():find("gun") or obj.Name:lower():find("key")) and not obj:FindFirstChild("ItemESP") then
                                 local part = obj:FindFirstChildWhichIsA("BasePart")
                                if part then
                                    local bb = Instance.new("BillboardGui", obj)
                                    bb.Name = "ItemESP"
                                    bb.Size = UDim2.new(0, 100, 0, 30)
                                    bb.AlwaysOnTop = true
                                    bb.Adornee = part
                                    local txt = Instance.new("TextLabel", bb)
                                     txt.Size = UDim2.new(1,0,1,0)
                                    txt.BackgroundTransparency = 1
                                    txt.Text = obj.Name
                                     txt.TextScaled = true
                                    txt.TextColor3 = Color3.fromRGB(220, 190, 255)
                                  end
                            end
                        end
                    end

                    local char = plr.Character
                    if char and char:FindFirstChild("Head") and char:FindFirstChild("HumanoidRootPart") then
                        if nameESP then
                            local gui = char.Head:FindFirstChild("ESP") or Instance.new("BillboardGui", char.Head)
                             gui.Name = "ESP"
                            gui.Size = UDim2.new(0,100,0,40)
                            gui.AlwaysOnTop = true
                            gui.StudsOffset = Vector3.new(0,2,0)
                            gui.Adornee = char.Head
                            
                            local txt = gui:FindFirstChild("TXT") or Instance.new("TextLabel", gui)
                            txt.Name = "TXT"
                            txt.Size = UDim2.new(1,0,1,0)
                            txt.BackgroundTransparency = 1
                            txt.TextColor3 = Color3.fromRGB(220, 190, 255)
                            txt.TextStrokeTransparency = 0
                            txt.Font = Enum.Font.GothamBold
                            txt.TextScaled = true
                            
                            local myChar = game.Players.LocalPlayer.Character
                            if myChar and myChar:FindFirstChild("HumanoidRootPart") then
                                local dist = math.floor((myChar.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Magnitude)
                                txt.Text = plr.Name .. "\n" .. dist .. "m"
                             end
                        else
                            if char.Head:FindFirstChild("ESP") then char.Head.ESP:Destroy() end
                         end

                        if outlineESP then
                            if not char:FindFirstChild("ESPHighlight") then
                                 local hl = Instance.new("Highlight", char)
                                 hl.Name = "ESPHighlight"
                                hl.FillColor = Color3.fromRGB(220, 190, 255)
                                hl.FillTransparency = 0.7
                                hl.OutlineColor = Color3.fromRGB(220, 190, 255)
                                hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                            end
                         else
                            if char:FindFirstChild("ESPHighlight") then char.ESPHighlight:Destroy() end
                         end
                    end
                 end
            end
        end
    end)
end

do
    local RejoinBtn = Instance.new("TextButton", ServerPage)
    RejoinBtn.Size = UDim2.new(1,-20,0,35)
    RejoinBtn.Position = UDim2.new(0,10,0,10)
    RejoinBtn.BackgroundColor3 = Color3.fromRGB(255,255,255)
    RejoinBtn.Text = ""
    applyGradient(RejoinBtn)
    Instance.new("UICorner", RejoinBtn)
    local rejLbl = Instance.new("TextLabel", RejoinBtn)
    rejLbl.Size = UDim2.new(1,0,1,0)
    rejLbl.BackgroundTransparency = 1
    rejLbl.Text = "🖱 Rejoin"
    rejLbl.Font = Enum.Font.GothamBold
    rejLbl.TextColor3 = Color3.new(1,1,1)

    local HopBtn = Instance.new("TextButton", ServerPage)
    HopBtn.Size = UDim2.new(1,-20,0,35)
    HopBtn.Position = UDim2.new(0,10,0,55)
    HopBtn.BackgroundColor3 = Color3.fromRGB(255,255,255)
    HopBtn.Text = ""
    applyGradient(HopBtn)
    Instance.new("UICorner", HopBtn)
    local hopLbl = Instance.new("TextLabel", HopBtn)
    hopLbl.Size = UDim2.new(1,0,1,0)
    hopLbl.BackgroundTransparency = 1
    hopLbl.Text = "🖱 Server Hop"
    hopLbl.Font = Enum.Font.GothamBold
    hopLbl.TextColor3 = Color3.new(1,1,1)

    local ServerWidget = Instance.new("Frame", ScreenGui)
    ServerWidget.Visible = false
    ServerWidget.Size = UDim2.new(0, 110, 0, 35)
    ServerWidget.Position = UDim2.new(0.5, -55, 0, 20)
    ServerWidget.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    applyGradient(ServerWidget)
    ServerWidget.BackgroundTransparency = 0.1
    Instance.new("UICorner", ServerWidget).CornerRadius = UDim.new(0, 8)
    local widgetStroke = Instance.new("UIStroke", ServerWidget)
    widgetStroke.Color = Color3.fromRGB(220, 190, 255)
    widgetStroke.Thickness = 1.2

    local ServerDot = Instance.new("Frame", ServerWidget)
    ServerDot.Size = UDim2.new(0, 10, 0, 10)
    ServerDot.Position = UDim2.new(0, 10, 0.5, -5)
    ServerDot.BackgroundColor3 = Color3.fromRGB(220, 190, 255)
    Instance.new("UICorner", ServerDot).CornerRadius = UDim.new(1, 0)

    local ServerWidgetBtn = Instance.new("TextButton", ServerWidget)
    ServerWidgetBtn.Size = UDim2.new(1, -25, 1, 0)
    ServerWidgetBtn.Position = UDim2.new(0, 25, 0, 0)
    ServerWidgetBtn.BackgroundTransparency = 1
    ServerWidgetBtn.Text = "Servers"
    ServerWidgetBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    ServerWidgetBtn.Font = Enum.Font.GothamBold
    ServerWidgetBtn.TextSize = 14

    makeDraggable(ServerWidget, ServerWidgetBtn)

    local ServerMenu = Instance.new("Frame", ScreenGui)
    ServerMenu.Visible = false
    ServerMenu.Size = UDim2.new(0, 380, 0, 260)
    ServerMenu.Position = UDim2.new(0.5, -190, 0.5, -130)
    ServerMenu.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    applyGradient(ServerMenu)
    ServerMenu.BackgroundTransparency = 0.1
    Instance.new("UICorner", ServerMenu).CornerRadius = UDim.new(0, 10)
    local menuStroke = Instance.new("UIStroke", ServerMenu)
    menuStroke.Color = Color3.fromRGB(210, 180, 255)
    menuStroke.Thickness = 1.2

    local SrvMenuTopBar = Instance.new("Frame", ServerMenu)
    SrvMenuTopBar.Size = UDim2.new(1, 0, 0, 30)
    SrvMenuTopBar.BackgroundTransparency = 1

    local SrvMenuTitle = Instance.new("TextLabel", SrvMenuTopBar)
    SrvMenuTitle.Size = UDim2.new(0, 120, 1, 0)
    SrvMenuTitle.Position = UDim2.new(0, 15, 0, 0)
    SrvMenuTitle.BackgroundTransparency = 1
    SrvMenuTitle.Text = "Server List"
    SrvMenuTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
    SrvMenuTitle.Font = Enum.Font.GothamBold
    SrvMenuTitle.TextXAlignment = Enum.TextXAlignment.Left

    makeDraggable(ServerMenu, SrvMenuTopBar)

    local SrvSortBtn = Instance.new("TextButton", SrvMenuTopBar)
    SrvSortBtn.Size = UDim2.new(0, 130, 0, 20)
    SrvSortBtn.Position = UDim2.new(1, -200, 0, 5)
    SrvSortBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
    SrvSortBtn.Text = "Sort: Ascending"
    SrvSortBtn.TextColor3 = Color3.new(1, 1, 1)
    SrvSortBtn.Font = Enum.Font.Gotham
    SrvSortBtn.TextSize = 12
    Instance.new("UICorner", SrvSortBtn).CornerRadius = UDim.new(0, 6)

    local SrvMinBtn = Instance.new("TextButton", SrvMenuTopBar)
    SrvMinBtn.Size = UDim2.new(0, 25, 0, 25)
    SrvMinBtn.Position = UDim2.new(1, -60, 0, 2)
    SrvMinBtn.BackgroundTransparency = 1
    SrvMinBtn.Text = "-"
    SrvMinBtn.TextColor3 = Color3.new(1,1,1)
    SrvMinBtn.TextSize = 18

    local SrvCloseBtn = Instance.new("TextButton", SrvMenuTopBar)
    SrvCloseBtn.Size = UDim2.new(0, 25, 0, 25)
    SrvCloseBtn.Position = UDim2.new(1, -30, 0, 2)
    SrvCloseBtn.BackgroundTransparency = 1
    SrvCloseBtn.Text = "X"
    SrvCloseBtn.TextColor3 = Color3.new(1,1,1)
    SrvCloseBtn.Font = Enum.Font.GothamBold

    local SrvListScroll = Instance.new("ScrollingFrame", ServerMenu)
    SrvListScroll.Size = UDim2.new(1, -20, 1, -40)
    SrvListScroll.Position = UDim2.new(0, 10, 0, 35)
    SrvListScroll.BackgroundTransparency = 1
    SrvListScroll.ScrollBarThickness = 4
    SrvListScroll.BorderSizePixel = 0

    local srvListLayout = Instance.new("UIListLayout", SrvListScroll)
    srvListLayout.Padding = UDim.new(0, 5)

    local srvSortAscending = true
    local srvMinimized = false

    local function populateServers()
        for _, v in pairs(SrvListScroll:GetChildren()) do
            if v:IsA("Frame") then v:Destroy() end
        end

        local success, result = pcall(function()
            return game:HttpGet("https://games.roblox.com/v1/games/" .. tostring(game.PlaceId) .. "/servers/Public?sortOrder=Asc&limit=100")
        end)

        if success and result then
             local decoded = HttpService:JSONDecode(result)
            if decoded and decoded.data then
                 local servers = {}
                for _, srv in pairs(decoded.data) do
                    if srv.playing and srv.maxPlayers and srv.playing < srv.maxPlayers then
                        table.insert(servers, srv)
                    end
                end

                table.sort(servers, function(a, b)
                    if srvSortAscending then
                         return a.playing < b.playing
                     else
                        return a.playing > b.playing
                    end
                 end)

                for _, srv in ipairs(servers) do
                     local item = Instance.new("Frame")
                    item.Size = UDim2.new(1, -10, 0, 35)
                    item.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
                    item.BackgroundTransparency = 0.5
                     Instance.new("UICorner", item).CornerRadius = UDim.new(0, 6)

                    local info = Instance.new("TextLabel", item)
                    info.Size = UDim2.new(0, 200, 1, 0)
                    info.Position = UDim2.new(0, 10, 0, 0)
                    info.BackgroundTransparency = 1
                    info.Text = "Players: " .. srv.playing .. "/" .. srv.maxPlayers .. "  | Ping: " .. (srv.ping or "?")
                    info.TextColor3 = Color3.new(1, 1, 1)
                    info.Font = Enum.Font.Gotham
                    info.TextSize = 13
                    info.TextXAlignment = Enum.TextXAlignment.Left

                     local joinBtn = Instance.new("TextButton", item)
                    joinBtn.Size = UDim2.new(0, 60, 0, 25)
                    joinBtn.Position = UDim2.new(1, -70, 0.5, -12.5)
                    joinBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                    joinBtn.Text = "" 
                    applyGradient(joinBtn)
                    Instance.new("UICorner", joinBtn).CornerRadius = UDim.new(0, 6)
                    
                    local joinLbl = Instance.new("TextLabel", joinBtn)
                    joinLbl.Size = UDim2.new(1,0,1,0)
                    joinLbl.BackgroundTransparency = 1
                    joinLbl.Text = "Join"
                    joinLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
                    joinLbl.Font = Enum.Font.GothamBold

                     joinBtn.MouseButton1Click:Connect(function()
                        showNotification("Подключение к новому серверу...")
                        TeleportService:TeleportToPlaceInstance(game.PlaceId, srv.id, game.Players.LocalPlayer)
                    end)

                    item.Parent = SrvListScroll
                 end
              end
        end
        SrvListScroll.CanvasSize = UDim2.new(0, 0, 0, srvListLayout.AbsoluteContentSize.Y + 10)
    end

    SrvSortBtn.MouseButton1Click:Connect(function()
        srvSortAscending = not srvSortAscending
        SrvSortBtn.Text = srvSortAscending and "Sort: Ascending" or "Sort: Descending"
        task.spawn(populateServers)
    end)

    SrvMinBtn.MouseButton1Click:Connect(function()
         srvMinimized = not srvMinimized
        if srvMinimized then
             ServerMenu:TweenSize(UDim2.new(0, 380, 0, 30), "Out", "Quad", 0.2, true)
            SrvListScroll.Visible = false
        else
            ServerMenu:TweenSize(UDim2.new(0, 380, 0, 260), "Out", "Quad", 0.2, true)
            SrvListScroll.Visible = true
         end
    end)

    SrvCloseBtn.MouseButton1Click:Connect(function()
        ServerMenu.Visible = false
    end)

    ServerWidgetBtn.MouseButton1Click:Connect(function()
        ServerMenu.Visible = not ServerMenu.Visible
        if ServerMenu.Visible then
            task.spawn(populateServers)
        end
    end)

    local ServerToggleLabel = Instance.new("TextLabel", ServerPage)
    ServerToggleLabel.Text = "Servers Toggle"
    ServerToggleLabel.Size = UDim2.new(0, 150, 0, 30)
    ServerToggleLabel.Position = UDim2.new(0, 10, 0, 100)
    ServerToggleLabel.BackgroundTransparency = 1
    ServerToggleLabel.TextColor3 = Color3.fromRGB(220, 190, 255)
    ServerToggleLabel.Font = Enum.Font.GothamBold
    ServerToggleLabel.TextXAlignment = Enum.TextXAlignment.Left

    local ToggleBG = Instance.new("TextButton", ServerPage)
    ToggleBG.Text = ""
    ToggleBG.Size = UDim2.new(0,60,0,30)
    ToggleBG.Position = UDim2.new(1,-75,0,100)
    ToggleBG.BackgroundColor3 = Color3.fromRGB(50,50,60)
    Instance.new("UICorner", ToggleBG).CornerRadius = UDim.new(1, 0)

    local ToggleCircle = Instance.new("Frame", ToggleBG)
    ToggleCircle.Size = UDim2.new(0,24,0,24)
    ToggleCircle.Position = UDim2.new(0,3,0.5,-12)
    ToggleCircle.BackgroundColor3 = Color3.new(1,1,1)
    Instance.new("UICorner", ToggleCircle).CornerRadius = UDim.new(1, 0)

    local toggleEnabled = false
    ToggleBG.MouseButton1Click:Connect(function()
        toggleEnabled = not toggleEnabled
        ServerWidget.Visible = toggleEnabled
        if not toggleEnabled then ServerMenu.Visible = false end
        setToggleState(ToggleBG, ToggleCircle, toggleEnabled, "Server Widget")
    end)

    RejoinBtn.MouseButton1Click:Connect(function()
        showNotification("Перезаход на сервер...")
        TeleportService:Teleport(game.PlaceId, game.Players.LocalPlayer)
    end)
end

do
    local PlayerCard = Instance.new("Frame", HomePage)
    PlayerCard.Size = UDim2.new(1,-10,0,80)
    PlayerCard.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    applyGradient(PlayerCard)
    PlayerCard.BackgroundTransparency = .15
    Instance.new("UICorner", PlayerCard)

    local Avatar = Instance.new("ImageLabel", PlayerCard)
    Avatar.Size = UDim2.new(0,55,0,55)
    Avatar.Position = UDim2.new(0,10,0.5,-27)
    Avatar.BackgroundTransparency = 1
    pcall(function()
         Avatar.Image = game.Players:GetUserThumbnailAsync(game.Players.LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
    end)

    local Welcome = Instance.new("TextLabel", PlayerCard)
    Welcome.Position = UDim2.new(0,75,0,20)
    Welcome.Size = UDim2.new(1,-80,0,30)
    Welcome.BackgroundTransparency = 1
    Welcome.Text = "Welcome, " .. game.Players.LocalPlayer.Name
    Welcome.TextColor3 = Color3.fromRGB(255,255,255)
    Welcome.Font = Enum.Font.GothamBold
    Welcome.TextSize = 18
    Welcome.TextXAlignment = Enum.TextXAlignment.Left

    local Status = Instance.new("Frame", HomePage)
    Status.Position = UDim2.new(0,0,0,95)
    Status.Size = UDim2.new(1,-10,0,160)
    Status.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    applyGradient(Status)
    Status.BackgroundTransparency = .15
    Instance.new("UICorner", Status)

    local StatusTitle = Instance.new("TextLabel", Status)
    StatusTitle.Text = "Status"
    StatusTitle.Position = UDim2.new(0,10,0,5)
    StatusTitle.Size = UDim2.new(1,0,0,20)
    StatusTitle.BackgroundTransparency = 1
    StatusTitle.TextColor3 = Color3.fromRGB(255,255,255)
    StatusTitle.Font = Enum.Font.GothamBold
    StatusTitle.TextXAlignment = Enum.TextXAlignment.Left

    local GameLabel = Instance.new("TextLabel", Status)
    local FPSLabel = Instance.new("TextLabel", Status)
    local PingLabel = Instance.new("TextLabel", Status)
    local PlayersLabel = Instance.new("TextLabel", Status)

    for i, v in pairs({GameLabel, FPSLabel, PingLabel, PlayersLabel}) do
        v.Position = UDim2.new(0, 10, 0, 30 + (i * 25))
        v.Size = UDim2.new(1,-20, 0,20)
        v.BackgroundTransparency = 1
        v.TextColor3 = Color3.fromRGB(255,255,255)
        v.Font = Enum.Font.Gotham
        v.TextXAlignment = Enum.TextXAlignment.Left
    end

    pcall(function()
        GameLabel.Text = "Game      " .. game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name
    end)

    task.spawn(function()
        while true do
            FPSLabel.Text = "FPS     " .. math.floor(workspace:GetRealPhysicsFPS())
            local ping = math.floor(game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue())
            PingLabel.Text = "Ping     " .. ping .. " ms"
            PlayersLabel.Text = "Players      " .. #game.Players:GetPlayers()
            task.wait(.5)
        end
    end)
end

local currentPage = HomePage
HomeBtn.Indicator.BackgroundTransparency = 0 

local function switchTab(btn, page)
    if Frame.Size.Y.Offset < 100 or currentPage == page then return end 

    for _, b in pairs(SideMenuScroll:GetChildren()) do
        if b:IsA("TextButton") and b:FindFirstChild("Indicator") then
             TweenService:Create(b.Indicator, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
        end
    end
    
    TweenService:Create(btn.Indicator, TweenInfo.new(0.3), {BackgroundTransparency = 0}):Play()

    if currentPage then
        local oldPage = currentPage
        TweenService:Create(oldPage, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Position = UDim2.new(0, 110, 0, 50)}):Play()
        task.delay(0.2, function()
            if currentPage ~= oldPage then oldPage.Visible = false end
        end)
    end

    page.Visible = true
    page.Position = UDim2.new(0, 110, 0, 30) 
    TweenService:Create(page, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Position = UDim2.new(0, 110, 0, 40)}):Play()
    currentPage = page
end

HomeBtn.MouseButton1Click:Connect(function() switchTab(HomeBtn, HomePage) end)
ServerBtn.MouseButton1Click:Connect(function() switchTab(ServerBtn, ServerPage) end)
VisualsBtn.MouseButton1Click:Connect(function() switchTab(VisualsBtn, VisualsPage) end)
TeleportBtn.MouseButton1Click:Connect(function() switchTab(TeleportBtn, TeleportPage) end)
MovedBtn.MouseButton1Click:Connect(function() switchTab(MovedBtn, MovedPage) end)
MainMiscBtn.MouseButton1Click:Connect(function() switchTab(MainMiscBtn, MainMiscPage) end)
SettingsBtn.MouseButton1Click:Connect(function() switchTab(SettingsBtn, SettingsPage) end)

local Title = Instance.new("TextLabel", Frame)
Title.Size = UDim2.new(0, 180, 0, 30)
Title.Position = UDim2.new(0, 12, 0, 4)
Title.BackgroundTransparency = 1
Title.Text = "SEPTUS HUB"
Title.TextColor3 = Color3.fromRGB(220, 190, 255)
Title.TextSize = 20
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left

local InjectorTag = Instance.new("TextLabel", Frame)
local executor = "Unknown"
pcall(function()
    if identifyexecutor then executor = identifyexecutor()
    elseif getexecutorname then executor = getexecutorname() end
end)
InjectorTag.Size = UDim2.new(0, 90, 0, 20)
InjectorTag.Position = UDim2.new(1, -145, 0, 6)
InjectorTag.BackgroundColor3 = Color3.fromRGB(220,190,255)
InjectorTag.Text = executor
InjectorTag.TextColor3 = Color3.fromRGB(20,20,25)
InjectorTag.TextSize = 11
InjectorTag.Font = Enum.Font.GothamBold
Instance.new("UICorner", InjectorTag).CornerRadius = UDim.new(0,8)

MinBtn.Parent = Frame
MinBtn.Size = UDim2.new(0, 20, 0, 20)
MinBtn.Position = UDim2.new(1, -50, 0, 6)
MinBtn.Text = "-"
MinBtn.TextColor3 = Color3.fromRGB(255,255,255)
MinBtn.TextSize = 18
MinBtn.BackgroundTransparency = 1

local minimized = false
MinBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    if minimized then
        Frame:TweenSize(UDim2.new(0,437,0,35), "Out", "Quad", 0.2, true)
        SideMenuScroll.Visible = false
        SideBar.Visible = false
        if currentPage then currentPage.Visible = false end
    else
        Frame:TweenSize(UDim2.new(0,437,0,238), "Out", "Quad", 0.2, true)
        SideMenuScroll.Visible = true
        SideBar.Visible = true
        if currentPage then currentPage.Visible = true end
    end
end)

CloseBtn.Parent = Frame
CloseBtn.Size = UDim2.new(0, 20, 0, 20)
CloseBtn.Position = UDim2.new(1, -25, 0, 6)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255,255,255)
CloseBtn.TextSize = 14
CloseBtn.BackgroundTransparency = 1

CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

-- === ДЕКОРАЦИИ И ЛОГИКА РАЗДЕЛОВ SETTINGS ===
local themeColorsOpened = false
local notifSystemOpened = false

-- Главная раскрывающаяся кнопка Theme Colors
local ThemeColorsBtn, ThemeArrow, ThemeLbl = createMenuBtn(SettingsPage, "Theme Colors", 10, true)

-- Панель под список градиентов
local ThemeColorsPanel = Instance.new("Frame", SettingsPage)
ThemeColorsPanel.Size = UDim2.new(1, -20, 0, 0)
ThemeColorsPanel.Position = UDim2.new(0, 10, 0, 50)
ThemeColorsPanel.BackgroundTransparency = 1
ThemeColorsPanel.ClipsDescendants = true

local ThemeListLayout = Instance.new("UIListLayout", ThemeColorsPanel)
ThemeListLayout.Padding = UDim.new(0, 5)

local function createThemeOption(name, c1, c2)
    local btn = Instance.new("TextButton", ThemeColorsPanel)
    btn.Size = UDim2.new(1, 0, 0, 30)
    btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    btn.Text = ""
    
    local grad = Instance.new("UIGradient", btn)
    grad.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, c1), ColorSequenceKeypoint.new(1, c2)})
    grad.Rotation = 45
    Instance.new("UICorner", btn)
    
    local lbl = Instance.new("TextLabel", btn)
    lbl.Size = UDim2.new(1, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = name
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 13
    lbl.TextColor3 = Color3.new(1, 1, 1)
    
    btn.MouseButton1Click:Connect(function()
        _G.changeTheme(c1, c2)
    end)
end

-- 10 уникальных градиентных тем по твоему запросу
createThemeOption("Black & Orange", Color3.fromRGB(15, 15, 20), Color3.fromRGB(255, 120, 0))
createThemeOption("Black & Red", Color3.fromRGB(15, 15, 20), Color3.fromRGB(255, 50, 50))
createThemeOption("Black & Green", Color3.fromRGB(15, 15, 20), Color3.fromRGB(50, 255, 50))
createThemeOption("Black & Blue", Color3.fromRGB(15, 15, 20), Color3.fromRGB(50, 100, 255))
createThemeOption("Black & Yellow", Color3.fromRGB(15, 15, 20), Color3.fromRGB(255, 215, 0))
createThemeOption("Black & Cyan", Color3.fromRGB(15, 15, 20), Color3.fromRGB(0, 255, 255))
createThemeOption("Black & Pink", Color3.fromRGB(15, 15, 20), Color3.fromRGB(255, 105, 180))
createThemeOption("Black & White", Color3.fromRGB(15, 15, 20), Color3.fromRGB(240, 240, 240))
createThemeOption("Indigo Night & Gold", Color3.fromRGB(20, 10, 40), Color3.fromRGB(255, 200, 50))
createThemeOption("Deep Aqua & Mint", Color3.fromRGB(10, 35, 40), Color3.fromRGB(100, 255, 180))

-- Функция Сброса темы (Reset the theme)
local ResetThemeBtn = Instance.new("TextButton", ThemeColorsPanel)
ResetThemeBtn.Size = UDim2.new(1, 0, 0, 30)
ResetThemeBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
ResetThemeBtn.Text = "Reset the theme"
ResetThemeBtn.Font = Enum.Font.GothamBold
ResetThemeBtn.TextSize = 13
ResetThemeBtn.TextColor3 = Color3.fromRGB(220, 190, 255)
Instance.new("UICorner", ResetThemeBtn)

ResetThemeBtn.MouseButton1Click:Connect(function()
    _G.changeTheme(Color3.fromRGB(15, 15, 20), Color3.fromRGB(130, 90, 180))
end)

-- Главная раскрывающаяся кнопка Notification system
local NotifSystemBtn, NotifArrow, NotifLbl = createMenuBtn(SettingsPage, "Notification system", 55, true)

-- Панель под систему уведомлений
local NotifSystemPanel = Instance.new("Frame", SettingsPage)
NotifSystemPanel.Size = UDim2.new(1, -20, 0, 0)
NotifSystemPanel.Position = UDim2.new(0, 10, 0, 95)
NotifSystemPanel.BackgroundTransparency = 1
NotifSystemPanel.ClipsDescendants = true

local NotifListLayout = Instance.new("UIListLayout", NotifSystemPanel)
NotifListLayout.Padding = UDim.new(0, 5)

-- Элемент переключателя (Вкл/Выкл)
local ToggleCont = Instance.new("Frame", NotifSystemPanel)
ToggleCont.Size = UDim2.new(1, 0, 0, 35)
ToggleCont.BackgroundTransparency = 1

local NotifToggle, NotifCircle = createToggle(ToggleCont, "Enable Notifications", 0)
NotifToggle.Parent = ToggleCont
NotifToggle.Position = UDim2.new(1, -65, 0.5, -15)

local CustomToggleLbl = Instance.new("TextLabel", ToggleCont)
CustomToggleLbl.Text = "Notification System Status"
CustomToggleLbl.Size = UDim2.new(0, 180, 1, 0)
CustomToggleLbl.BackgroundTransparency = 1
CustomToggleLbl.TextColor3 = Color3.fromRGB(220, 190, 255)
CustomToggleLbl.Font = Enum.Font.Gotham
CustomToggleLbl.TextXAlignment = Enum.TextXAlignment.Left

setToggleState(NotifToggle, NotifCircle, true)

NotifToggle.MouseButton1Click:Connect(function()
    _G.NotificationsEnabled = not _G.NotificationsEnabled
    setToggleState(NotifToggle, NotifCircle, _G.NotificationsEnabled)
end)

-- Кнопка для открытия меню кастомизации маскота
local OpenCustBtn = Instance.new("TextButton", NotifSystemPanel)
OpenCustBtn.Size = UDim2.new(1, 0, 0, 35)
OpenCustBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
OpenCustBtn.Text = ""
applyGradient(OpenCustBtn)
Instance.new("UICorner", OpenCustBtn)

local OpenCustLbl = Instance.new("TextLabel", OpenCustBtn)
OpenCustLbl.Size = UDim2.new(1, 0, 1, 0)
OpenCustLbl.BackgroundTransparency = 1
OpenCustLbl.Text = "Character Customization Menu"
OpenCustLbl.Font = Enum.Font.GothamBold
OpenCustLbl.TextSize = 13
OpenCustLbl.TextColor3 = Color3.new(1, 1, 1)

-- Функция динамической перестройки высоты страницы Настроек
local function refreshSettingsLayout()
    local themeH = themeColorsOpened and ThemeListLayout.AbsoluteContentSize.Y + 10 or 0
    local notifH = notifSystemOpened and NotifListLayout.AbsoluteContentSize.Y + 10 or 0
    
    TweenService:Create(ThemeColorsPanel, TweenInfo.new(0.2), {Size = UDim2.new(1, -20, 0, themeH)}):Play()
    TweenService:Create(NotifSystemBtn, TweenInfo.new(0.2), {Position = UDim2.new(0, 10, 0, 50 + themeH)}):Play()
    TweenService:Create(NotifSystemPanel, TweenInfo.new(0.2), {Position = UDim2.new(0, 10, 0, 95 + themeH), Size = UDim2.new(1, -20, 0, notifH)}):Play()
    
    SettingsPage.CanvasSize = UDim2.new(0, 0, 0, 110 + themeH + notifH)
end

ThemeColorsBtn.MouseButton1Click:Connect(function()
    themeColorsOpened = not themeColorsOpened
    ThemeArrow.Text = themeColorsOpened and "▲" or "▼"
    refreshSettingsLayout()
end)

NotifSystemBtn.MouseButton1Click:Connect(function()
    notifSystemOpened = not notifSystemOpened
    NotifArrow.Text = notifSystemOpened and "▲" or "▼"
    refreshSettingsLayout()
end)


-- === ОТДЕЛЬНОЕ МАЛЕНЬКОЕ ОКНО CHARACTER CUSTOMIZATION ===
local CharCustWin = Instance.new("Frame", ScreenGui)
CharCustWin.Size = UDim2.new(0, 300, 0, 150)
CharCustWin.Position = UDim2.new(0.5, -150, 0.5, -75)
CharCustWin.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
CharCustWin.Visible = false
CharCustWin.BorderSizePixel = 0
applyGradient(CharCustWin)
Instance.new("UICorner", CharCustWin).CornerRadius = UDim.new(0, 10)

local CustStroke = Instance.new("UIStroke", CharCustWin)
CustStroke.Color = Color3.fromRGB(210, 180, 255)
CustStroke.Thickness = 1.2

local CustTopBar = Instance.new("Frame", CharCustWin)
CustTopBar.Size = UDim2.new(1, 0, 0, 30)
CustTopBar.BackgroundTransparency = 1

local CustTitle = Instance.new("TextLabel", CustTopBar)
CustTitle.Size = UDim2.new(0, 180, 1, 0)
CustTitle.Position = UDim2.new(0, 10, 0, 0)
CustTitle.BackgroundTransparency = 1
CustTitle.Text = "Character Customization"
CustTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
CustTitle.Font = Enum.Font.GothamBold
CustTitle.TextSize = 13
CustTitle.TextXAlignment = Enum.TextXAlignment.Left

makeDraggable(CharCustWin, CustTopBar)

-- Кнопка Сворачивания (-)
local CustMinBtn = Instance.new("TextButton", CustTopBar)
CustMinBtn.Size = UDim2.new(0, 25, 0, 25)
CustMinBtn.Position = UDim2.new(1, -55, 0, 2)
CustMinBtn.BackgroundTransparency = 1
CustMinBtn.Text = "-"
CustMinBtn.TextColor3 = Color3.new(1, 1, 1)
CustMinBtn.TextSize = 18

-- Кнопка Закрытия (X)
local CustCloseBtn = Instance.new("TextButton", CustTopBar)
CustCloseBtn.Size = UDim2.new(0, 25, 0, 25)
CustCloseBtn.Position = UDim2.new(1, -30, 0, 2)
CustCloseBtn.BackgroundTransparency = 1
CustCloseBtn.Text = "X"
CustCloseBtn.TextColor3 = Color3.new(1, 1, 1)
CustCloseBtn.Font = Enum.Font.GothamBold

-- Тело контента меню
local CustContent = Instance.new("Frame", CharCustWin)
CustContent.Size = UDim2.new(1, 0, 1, -30)
CustContent.Position = UDim2.new(0, 0, 0, 30)
CustContent.BackgroundTransparency = 1

-- Текстовое Поле Ввода Asset ID
local AssetTextBox = Instance.new("TextBox", CustContent)
AssetTextBox.Size = UDim2.new(1, -20, 0, 35)
AssetTextBox.Position = UDim2.new(0, 10, 0, 10)
AssetTextBox.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
AssetTextBox.Text = ""
AssetTextBox.PlaceholderText = "Paste Asset ID here..."
AssetTextBox.TextColor3 = Color3.new(1, 1, 1)
AssetTextBox.Font = Enum.Font.Gotham
AssetTextBox.TextSize = 13
Instance.new("UICorner", AssetTextBox).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AssetTextBox).Color = Color3.fromRGB(60, 60, 70)

AssetTextBox:GetPropertyChangedSignal("Text"):Connect(function()
    local val = AssetTextBox.Text
    if val ~= "" then
        if tonumber(val) then
            _G.NotificationAssetID = "rbxthumb://type=Asset&id=" .. val .. "&w=150&h=150"
        else
            _G.NotificationAssetID = val
        end
    end
end)

-- Кнопка Reset (сброс к исходному маскоту)
local ResetAssetBtn = Instance.new("TextButton", CustContent)
ResetAssetBtn.Size = UDim2.new(1, -20, 0, 35)
ResetAssetBtn.Position = UDim2.new(0, 10, 0, 55)
ResetAssetBtn.BackgroundColor3 = Color3.fromRGB(50, 45, 60)
ResetAssetBtn.Text = "Reset Asset ID"
ResetAssetBtn.Font = Enum.Font.GothamBold
ResetAssetBtn.TextSize = 13
ResetAssetBtn.TextColor3 = Color3.fromRGB(220, 190, 255)
Instance.new("UICorner", ResetAssetBtn).CornerRadius = UDim.new(0, 6)

ResetAssetBtn.MouseButton1Click:Connect(function()
    _G.NotificationAssetID = "rbxthumb://type=Asset&id=115669221263328&w=150&h=150"
    AssetTextBox.Text = ""
    showNotification("Asset ID возвращен по умолчанию!")
end)

-- Управление окном кастомизации
OpenCustBtn.MouseButton1Click:Connect(function() CharCustWin.Visible = not CharCustWin.Visible end)
CustCloseBtn.MouseButton1Click:Connect(function() CharCustWin.Visible = false end)

local custMinimized = false
CustMinBtn.MouseButton1Click:Connect(function()
    custMinimized = not custMinimized
    if custMinimized then
        CharCustWin:TweenSize(UDim2.new(0, 300, 0, 30), "Out", "Quad", 0.2, true)
        CustContent.Visible = false
    else
        CharCustWin:TweenSize(UDim2.new(0, 300, 0, 150), "Out", "Quad", 0.2, true)
        CustContent.Visible = true
    end
end)

local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size = UDim2.new(0,50,0,50)
ToggleBtn.Position = UDim2.new(0,50,0.5,-25)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Text = ""
applyGradient(ToggleBtn)
ToggleBtn.Visible = false

local toggleLbl = Instance.new("TextLabel", ToggleBtn)
toggleLbl.Size = UDim2.new(1,0,1,0)
toggleLbl.BackgroundTransparency = 1
toggleLbl.Text = "SH"
toggleLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleLbl.TextSize = 20
toggleLbl.Font = Enum.Font.GothamBold

local ToggleCorner = Instance.new("UICorner", ToggleBtn)
ToggleCorner.CornerRadius = UDim.new(0,12)
local ToggleStroke = Instance.new("UIStroke", ToggleBtn)
ToggleStroke.Color = Color3.fromRGB(240,220,255)

task.spawn(function()
    -- Ожидаем, пока завершится цепочка: Приветствие -> Загрузочный бар
    repeat task.wait() until _G.MainMenuLoaded == true
    ToggleBtn.Visible = true
    showNotification("SEPTUS HUB успешно загружен!")
end)

local isGuiVisible = true
ToggleBtn.MouseButton1Click:Connect(function()
    isGuiVisible = not isGuiVisible
    if isGuiVisible then
        Frame.Visible = true
         TweenService:Create(Frame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = minimized and UDim2.new(0, 437, 0, 35) or UDim2.new(0, 437, 0, 238),
            Position = UDim2.new(0.5, -218.5, 0.5, -119)
        }):Play()
    else
        TweenService:Create(Frame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
            Size = UDim2.new(0, 0, 0, 0),
            Position = UDim2.new(0.5, 0, 0.5, 0)
        }):Play()
        task.delay(0.4, function() 
            if not isGuiVisible then Frame.Visible = false end 
        end)
    end
end)

makeDraggable(ToggleBtn)
